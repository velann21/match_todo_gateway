apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gateway-srv.fullname" . }}-test-connection"
  labels:
    {{- include "gateway-srv.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gateway-srv.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
